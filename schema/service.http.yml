description:
  Enable the webserver with the on-boarding webui
type: object
properties:
  http-port:
    description:
      The port that the HTTP server should run on.
    type: integer
    maximum: 65535
    minimum: 1
    default: 80
