description:
  RRM/steering
type: object
properties:
  steer:
    description:
      This section describes the band steering behaviour of the unit.
    type: object
    properties:
      enabled:
        description:
          Enable this option to tell the unit that it shall paticipate in the netwrok wide steering
        type: number
        maximum: 1
        minimum: 0
      network:
        description:
          The network that shall be used to communicate with the other peers.
        type: string
