type: object
properties:
  ntp:
    type: object
    properties:
      description:
        description:
          The content that we shall announce inside the description field.
	type: string
      location:
        description:
          The content that we shall announce inside the location field.
	type: string
      interface:
        description:
          The interfaces that we want to run lldp on.
	type: array
