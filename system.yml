type: object
properties:
  system:
    type: object
    properties:
      hostname:
        description:
          This allows you to change the hostname of the device.
        type: string
      timezone:
        description:
          This allows you to change the TZ of the device.
        type: string
